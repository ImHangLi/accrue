# Keep compensation data local

Accrue can collect **Product Analytics** for product analysis, but compensation data stays local unless a user explicitly enables account sync later. Salary inputs, accrued amounts, and exact working schedules are sensitive enough that uploading them by default would weaken the product's trust boundary. Analytics does not interrupt setup, but settings must include a way to disable it.
