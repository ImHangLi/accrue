# Default to Calm Mode

Accrue defaults the menu bar to **Calm Mode**, showing only the current **Accrued Amount**, while keeping **Rate Mode** optional. This keeps the app close to an ambient display: useful at a glance, low-pressure during work, and able to reveal the live rate only when the user asks for stronger feedback.
