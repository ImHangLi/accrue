# Ship macOS first with a shared domain

Accrue v1 ships only as a macOS menu bar app, while keeping accrual calculation and shared concepts outside the Mac UI layer. This avoids early iOS and Live Activity complexity but leaves a clean path for future iPhone, Dynamic Island, and Live Activity support.
