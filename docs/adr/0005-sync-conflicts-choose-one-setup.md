# Resolve sync conflicts by choosing one setup

When an **Anonymous Install** signs into an **Optional Account** that already has setup data, Accrue asks the user to choose either the local setup or the account setup. It does not merge configurations because the synced data is small, user-facing, and easier to trust when one source clearly wins.
