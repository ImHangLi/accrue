# Use SwiftData for core configuration

Accrue stores core configuration in SwiftData and keeps small device-local preferences in AppStorage or UserDefaults. This gives the app a modern, structured local model for pay rules and work schedules while keeping incidental Mac-specific state lightweight.
